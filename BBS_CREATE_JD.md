## BBS Create JD

The tool to create JD on BBS using tool `w3connect`.

Steps:

    1. Create a post with staking


Create a post with staking on BBS (HTTP POST)
```bash
curl http://127.0.0.1:5333/bbs/create_post
```
Parameters:

    No parameters required.
